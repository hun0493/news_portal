name: news_portal
description: "뉴스 포탈 앱앱"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  go_router: ^13.2.0
  easy_localization: ^3.0.3

  # 📡 HTTP 요청 처리
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/translations/ # 🌐 easy_localization용 번역 파일 위치
